<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="post">
	
	<insert id="insert" parameterType="hashMap" useGeneratedKeys="true"
		keyProperty="UID">  
	    <![CDATA[
	    insert into Post
	    (postID,postCategoryID,userName,title,content,suggestion,views,updateDate,modifyDate,writer) 
	    values
	    (#{PostId},#{PostCateGoryID},#{userName},#{title},#{Content},#{suggestion},#{views},#{UpdateDate},#{modifyDate}, #{writer})
	]]>
	</insert>
	
	
	<insert id="save" parameterType="hashMap" useGeneratedKeys="true"
		keyProperty="UID">  
	    <![CDATA[
	    insert into Post
	    (postID,postCategoryID,userName,title,content,suggestion,views,updateDate,modifyDate,writer) 
	    values
	    (#{PostId},#{PostCateGoryID},#{userName},#{title},#{Content},#{suggestion},#{views},#{UpdateDate},#{modifyDate}, #{writer})
	]]>
	</insert>

	<sql id="search">
        <if test="type != null and keyword != null">
            <trim prefix="(" suffix=") AND " prefixOverrides="OR">
                <foreach item="type" collection="typeArr">
                    <trim prefix="OR">
                        <choose>
                            <when test="type =='t'.toString()">
                                (TITLE LIKE '%'||#{keyword})||'%')
                            </when>
                            <when test="type =='c'.toString()">
                                (CONTENT LIKE '%'||#{keyword})||'%')
                            </when>
                            <when test="type =='w'.toString()">
                                (WRITER LIKE '%'||#{keyword})||'%')
                            </when>
                        </choose>
                    </trim>
                </foreach>
            </trim>
        </if>
    </sql>
	
	<!--    게시글 목록 가져오기-->
    <select id="getList" resultType="com.example.test.POST.DTO.PostDTO">
        <![CDATA[
        SELECT PostID, PostCateGoryID, userName, title, Content, suggestion, views, updateDate, modifyDate, writer 
        FROM
        (
            SELECT /*+ INDEX_DESC(TBL_Post PK_Post) */
            ROWNUM R, PostID, PostCateGoryID, userName, title, Content, suggestion, views, updateDate, modifyDate, writer
            FROM TBL_Post
            WHERE ROWNUM <= #{pageNum} * #{amount}
        )
           WHERE R> (#{pageNum}-1) * #{amount}
        ]]>

    </select>
    
    <select id="getTotal" resultType="int">
       <![CDATA[
        SELECT COUNT(PostID) FROM TBL_Post
       <where>
         <include refid="search"></include>
       </where>
       ]]>
    </select>
	
	<select id="findByPostCategoryID" resultType="hashmap">
	 <![CDATA[
		SELECT PostCateGoryID 
		FROM Post
		WHERE postID = #{PostID} And userName=#{userName}; 
	]]>
	</select>

	<update id="updateCount">
	<![CDATA[
			Update Post 
			set
			views = #{views},
			where postID = #{PostID};
		]]>
	</update>
	
	
	<delete id="delete">
	<![CDATA[
		
			delete from Post 
			where userName = #{userName} and postID = #{PostID};	
	]]>
	</delete>
	






</mapper>